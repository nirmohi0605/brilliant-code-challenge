export const outputReportFile = `stock-report-${new Date().getTime()}.txt`;

// export const validCommandRegex = new RegExp(
//   /^register\s[a-zA-Z]+\s\$[0-9]+$/ |
//     /^checkin\s[a-zA-Z]+\s\$[0-9]+$/ |
//     /^order\s[a-zA-Z]+\s\$[0-9]+$/,
//   "i"
// );

// export const checkinRegex = new RegExp(/^register\s[a-zA-Z]+\s\$[0-9]+$/, "i");

// export const orderRegex = new RegExp(/^checkin\s[a-zA-Z]+\s\$[0-9]+$/, "i");

// export const registerRegex = new RegExp(/^order\s[a-zA-Z]+\s\$[0-9]+$/, "i");

// // export const testRegex = new RegExp(/^register\s\w+\s\$\d+\.\d+$/, "i");
// // (\.\d+)

// // export const testRegex = new RegExp(/^register \w+ \$\d+\.\d+$/, "i");

// export const testRegex = /^register$ ([a-zA-Z\d])+ \$[0-9]+\.[0-9]+$/;
